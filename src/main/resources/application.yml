spring:
  profiles:
    active: local
    group:
      local: mail
      dev: mail
---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/feelin?serverTimezone=Asia/Seoul
    username: feelin
    password: feelinuser
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: auth.feelin@gmail.com
    password: cubvolcblqwqorbv
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  jwt:
    secret: feelin-server-jwt-secret-key-2020
    expiration: 86400000
---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://database:3306/feelin?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: feelin
    password: feelin
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
